# Implementation Problem Log

This log is part of the acceptance evidence for `productize-agent-harness-experience`.

| ID | Stage | Status | Problem | Impact | Root Cause | Resolution | Verification | Residual Risk |
|---|---|---|---|---|---|---|---|---|
| P001 | Baseline | RESOLVED | Harness capabilities are spread across user and admin pages without a single product narrative. | A reviewer sees a generic assistant and CRUD console before seeing runtime engineering depth. | Navigation follows feature/database boundaries rather than the Agent Run lifecycle. | Adopt Reference App + Agent Harness + Control Plane information architecture and make Run evidence the shared spine. | Proposal and design define one canonical Run flow and staged UI acceptance criteria. | The benefit still requires Stage 1 browser verification. |
| P002 | Stage 1 | RESOLVED | Live Run evidence existed in SSE but `ChatPage` did not consume terminal `usage`, `status`, `terminationReason`, or `eventTime`. | The UI could not prove budgets, duration, termination, or event ordering even though the backend emitted them. | The frontend only handled the legacy `done` marker and kept a subset of event fields. | Added `runEvidence.js` to normalize operational events, coalesce budget updates, preserve terminal usage and suppress raw reflection content; `ChatPage` now attaches the evidence to each live answer. | Deterministic Node sample confirmed identity/usage/event behavior and `npm run build` passed on 2026-09-06. | A real provider SSE capture remains part of Stage 1 verification; historical reload is handled by tasks 1.4/1.5. |
| P003 | Baseline | RESOLVED | Durable Run query availability is coupled to the durable feature flag. | With the default flag off, `/api/v1/agent-runs/{runId}` cannot recover a completed Run after refresh. | The durable table is optional, although code persists every Run when enabled; Trace persists normal Run metadata independently but only has an admin API. | Keep the feature flag semantics and use an owner-only safe Trace projection for ordinary history; merge durable state when it exists. | Coordinator/store/Trace/security call-chain audit recorded in A005/A008 and design decision 1. | Trace write failure still means only live SSE evidence exists and must be labeled accordingly. |
| P004 | Baseline | RESOLVED | Existing Quality dashboard aggregates operational health but does not use fixed tasks and expected outcomes. | It cannot prove that a Harness/model change improved task completion. | Online Trace aggregation and offline regression evaluation are currently conflated in product language. | Added versioned datasets and an asynchronous Agent Eval runner that sends every fixed sample through the canonical Harness, then persists deterministic Outcome and Run references separately from the online Quality dashboard. | Static call-chain review and the six-metric hand calculation for task 2.8 confirm the separation; live execution remains part of Stage 2 verification. | Real business labels still require manual curation, and the management UI is task 2.9. |
| P005 | Baseline | MONITORING | Repository has many completed but unarchived OpenSpec changes and a large dirty worktree. | New edits can accidentally overwrite prior work or make scope attribution unclear. | Previous incremental changes were not archived after static verification. | Keep this change isolated, inspect diffs before every edit, and avoid destructive Git operations. | `git status` captured at proposal start; future task records list exact files. | Final repository cleanup remains outside this change unless separately approved. |
| P006 | Proposal | RESOLVED | Stage 4 task initially referenced `AgentToolRegistry` in the wrong package. | Apply would search or create the class under an incorrect ownership boundary. | The planning pass inferred the package from the conceptual module name instead of the authoritative file path. | Corrected the task to `com/ai/agent/tool/governance/AgentToolRegistry.java`. | `rg --files` confirms the corrected path exists. | Future planned paths must be resolved against the worktree before their task starts. |
| P007 | Baseline | RESOLVED | Conversation history cannot correlate an assistant answer with its Agent Run. | Reloading a session loses Run evidence even though Trace may exist. | `ConversationMessage` and `SessionManager.saveMessage` have no `runId`; `ChatPage` only keeps events in React state. | Stage 1 task 1.5 adds a nullable `run_id` and passes the canonical ID when saving assistant messages; legacy rows remain valid. | Static entity/recorder/DTO chain proves the gap; migration and runtime reload remain pending task 1.5. | Runs whose Trace write fails will retain the ID but may only have limited evidence. |
| P008 | Baseline | OPEN | `OverviewPage.jsx` is present but is not imported or routed by `App.jsx`. | It adds code and outdated product language without affecting the current UI. | Earlier dashboard work was disconnected when the user/admin shell was simplified. | Keep it out of the new information architecture; delete only after Stage 1 verifies no dynamic reference or intended route. | `rg OverviewPage frontend/src` finds only the component declaration. | Source cleanup is not required for the first visible Harness workflow. |
| P009 | Stage 1 | MONITORING | The running application redirects browser verification to the login screen and no authenticated test session is available. | User/admin navigation, the task-contract editor, Run Inspector and Agent Eval page cannot yet be visually verified against real data. | The browser-control session is intentionally isolated from user credentials and must not inspect or fabricate authentication storage. | Complete build and unauthenticated shell checks now; record authenticated desktop/mobile verification as `UNVERIFIED` until the user supplies an active session through normal login. | `http://localhost:8080/` rendered the login form with current assets and no browser console warning/error; Agent Eval production build produced `index-DfTLh49a.js` and `index-h8nMWCER.css` on 2026-09-06. | Layout regressions behind authentication may remain until Stage 1 and Stage 2 live verification. |
| P010 | Stage 1 | RESOLVED | Trace fallback preserves Run summary and selected operational evidence but not the complete SSE event stream. | A refreshed ordinary Run cannot honestly reconstruct every RAG, planning, budget and retry event. | `agent_execution_trace` stores Run metadata, Tool Journal and durable resume summaries, while most transient typed events are delivered only over SSE. | Added an owner-only Run evidence projection that merges durable state with Trace, omits raw arguments and hidden reasoning, and returns `eventHistoryComplete=false` for persisted projections. | Static owner matrix confirms `tenantId + userId` checks for both sources; DTO sensitive-field scan and `git diff --check` passed on 2026-09-06. Runtime Swagger verification awaits a backend restart and authenticated session. | Complete historical replay would require an explicitly designed bounded event store outside Stage 1 anti-scope. |
| P011 | Stage 1 | RESOLVED | The original message-to-Run task omitted the Coordinator and SessionManager call sites and mixed persistence with frontend reload. | Implementing it as written would either exceed the five-file task boundary or leave `runId` unpersisted/unread. | Planning followed the entity/DTO shape without tracing the complete write and reload paths. | Split the task into 1.5 persistence and 1.6 reload; use a nullable non-FK `run_id` so legacy messages and runs without Trace remain valid. | Call-site scan confirms only `AgentRunCoordinator -> AgentConversationRecorder -> SessionManager` writes completed conversation messages. | The end-to-end refresh path remains runtime-unverified until task 1.6 and an authenticated restart. |
| P012 | Stage 1 | RESOLVED | Loading evidence for every historical assistant message can create a request burst. | Long sessions could trigger up to 25 simultaneous Run detail requests after refresh. | The current API exposes owner Run detail by ID and conversation history returns up to 50 messages without a batch evidence endpoint. | Deduplicate Run IDs and hydrate in batches of four; isolate each failure so one missing Trace does not fail the conversation. | Frontend build and source review verify the concurrency bound and per-Run fallback. | A server-side batch endpoint may be justified if production telemetry shows this path is hot. |
| P013 | Stage 1 | RESOLVED | The running backend continued to serve the previous frontend asset manifest after a new Vite build. | Browser checks at `8080` did not exercise the latest UI. | Vite writes to `src/main/resources/static`, while the already-running Spring process serves copied resources from `target/classes/static`. | Synced the generated static directory into `target/classes/static` without compiling or restarting the backend. | `8080` now references `index-DzZQmy1F.js`; the asset returns HTTP 200. | Old hashed files remain harmlessly in generated `target`; a clean package will remove them later. |
| P014 | Stage 2 | RESOLVED | The initial Outcome propagation task omitted encrypted Checkpoint call sites and proposed redundant plaintext durable fields. | Approval resume could lose the task contract, while duplicating full goals/expected values would widen sensitive-data exposure. | The first task list treated Run, Trace and durable state as interchangeable persistence surfaces. | Keep the immutable full contract in RunContext and encrypted Checkpoint v2; store only safe criterion/evaluation projections in Trace, which the owner Run API already merges. | Constructor/call-site audit identified Coordinator and Restorer Run creation plus Suspension Checkpoint creation; implementation is task 2.2. | Old v1 Checkpoints restore with no task contract and therefore remain `NOT_EVALUATED`. |
| P015 | Stage 2 | RESOLVED | A bounded Tool Journal can omit earlier calls after overflow, making absence-based criteria unsafe. | `TOOL_CALLED` could be reported failed even though the required tool was called before the retained window. | Initial evaluator evidence modeled only observed names, not evidence completeness. | Added explicit tool/approval completeness flags; an absent match with incomplete evidence returns `NOT_EVALUATED`. Non-terminal Runs are also never evaluated as achieved. | Static branch matrix covers missing contract/evidence, match, mismatch, invalid JSON rule, incomplete Journal and non-terminal Run. | Live wiring must set completeness from authoritative overflow/state signals in task 2.4. |
| P016 | Stage 2 | RESOLVED | Approval resume reconstructs a fresh bounded Tool Journal and does not recover pre-pause execution records. | Absence-based tool or approval criteria could falsely fail after a successful resume. | Checkpoint intentionally preserves executable state and the task contract, not the complete operational journal. | Resume evaluation uses the original encrypted Checkpoint contract, current-segment positive evidence and the authoritative approval decision, while marking cross-segment tool and approval evidence incomplete. The safe Outcome is appended to the original Trace. | Static success/failure/v1 branch review confirms positive matches can pass and absent cross-segment evidence becomes `NOT_EVALUATED`. | A live approval pause/resume still requires durable mode, an authenticated owner and a backend restart. |
| P017 | Stage 2 | RESOLVED | A queued Eval could otherwise mix samples executed against different Agent Profile revisions. | Aggregate metrics would not represent one reproducible Agent configuration. | `agent_profile` is mutable and has no immutable version row. | Snapshot `updatedAt` as `agentProfileVersion`, verify it when the worker claims the run and before every sample, and stop the run if it changes; also freeze the enabled sample list at claim time and reject count drift. | Static state and call-chain review confirms drift changes the Eval Run to `FAILED` instead of mixing later samples. | There remains a narrow check-to-use race until Agent Profiles gain immutable version rows. |
| P018 | Stage 2 | OPEN | Configured-Agent Trace does not persist sample-level RAG citation IDs. | A report cannot honestly calculate RAG reference hit rate or provide sample-level retrieval evidence. | Configured Chat/ReAct retrieves RAG context, but the safe Trace payload currently persists Run, Tool Journal and Outcome rather than RAG citations. | Keep `ragReferenceHitRate` as a separate, explicit unavailable metric with reason `RAG_SAMPLE_EVIDENCE_NOT_PERSISTED`; do not infer it from task completion. | DTO and metric calculator review confirms RAG is separate and never reported as zero. | Stage 3 evidence work must add bounded safe citation identifiers before this metric becomes available. |
| P019 | Stage 2 | RESOLVED | Long Eval datasets initially shared the interactive `agentTaskExecutor`. | Offline regression could delay normal Orchestrator work and distort production latency. | The first implementation reused the nearest context-propagating executor instead of isolating workload classes. | Added a dedicated bounded `agentEvalExecutor` that retains MDC and Spring Security propagation; samples remain serial within each Eval Run. | Static bean/qualifier review confirms Eval submission no longer uses the Orchestrator executor. | Pool sizes are safe defaults and need load-based tuning in a real deployment. |
| P020 | Stage 2 | MONITORING | “无效循环”目前只有迭代上限终止这一种权威信号。 | 指标可以发现耗尽迭代预算的循环，但不能识别尚未触顶的重复工具参数或低收益往返。 | Harness 已有原子迭代预算和稳定 `ITERATION_LIMIT`，尚未实现语义级循环指纹。 | 第一版只把健康样本中观察到的 `ITERATION_LIMIT` 计为无效循环，保持定义确定且可复现。 | Metric calculator 与样本证据均从 Run `terminationReason` 读取，不根据回答文本猜测。 | 后续可在 Tool Journal 增加脱敏调用指纹和重复阈值，再扩展该指标定义并提升 evaluator 版本。 |
| P021 | Stage 3 | RESOLVED | `ReActPromptBuilder` 使用固定 8000 Token 从后向前静默截断。 | 它不考虑模型窗口、Run 剩余预算或任务合同，并可能拆散 assistant tool call 与 tool result，造成不同执行模式的上下文行为不一致。 | 历史实现把窗口控制放在 ReAct 调用方，而不是统一模型调用边界。 | 由 `McpModelService` 前的 Context Governor 统一准入；保守 Reducer 按完整 tool protocol group 保留，`ReActModelCaller` 不再调用旧 PromptBuilder。 | 调用图扫描确认 Chat、ReAct、Orchestrator/专家均通过 `McpModelService` 的五个准入点；只有非 Run 的管理员连接探测直接调用临时模型。 | 未引用的 `ReActPromptBuilder` 仍是待清理遗留类；模型窗口使用可配置保守默认值，厂商元数据自动同步不在本阶段范围内。 |
| P022 | Stage 3 | RESOLVED | 原 3.3 文件清单只有事件适配器、Trace writer 和恢复 worker，缺少 Run owner 查询 DTO/投影与 Context Governor 证据来源。 | 只改原清单会让实时事件存在但历史页面拿不到结构化 Token 与保留消息数据，或者要求从日志反推。 | 规划时只沿写入侧枚举文件，没有追踪 `AgentRunEvidenceService -> RunEvent -> runEvidence.js` 的历史读取链。 | 将 3.3 拆成两个最多 5 文件的实现片：A 负责有界采集、SSE 与 Trace，B 负责恢复进度和 owner-safe 投影；RunEvent 只增加白名单 `details`。 | 前端生产构建、Node 语法检查、Trace 字段扫描和 8080 新资源加载均通过。 | 真实持久化内容与审批事件顺序仍需后端重启后的 Stage 3 演示确认。 |
| P023 | Stage 4 | RESOLVED | Tool、Skill 和 Agent 的身份、版本与可用性来自不同数据源，且 `SkillManager` 原来只保留运行时名称。 | 直接把三个列表拼给前端会混淆“已配置”和“可执行”；Profile 保存 `skillId` 而运行时只按名称注册还会导致 Skill Agent 找不到已选技能。 | 旧功能分别围绕 Tool 名、Skill 运行时名和 Agent 数据库 ID 演进，没有统一扩展合同。 | 新增只读 Capability Registry；持久化 Skill/Agent 提供稳定 ID、版本、租户和所有者，运行时 Registry 提供实际可用性。`SkillManager` 同时维护 `skillId` 与名称别名，启停和启动加载按稳定 ID 精确维护。 | 静态映射覆盖统一规格字段；`SkillAgentSpecialist` 与 `SkillExecutionService` 的既有 `findSkillByName(skillId)` 现可命中稳定 ID 别名。 | Skill 的自然语言自动匹配仍使用进程级名称集合；多租户语义检索需要后续按租户收窄候选。 |
| P024 | Stage 4 | RESOLVED | 旧 `/available-tools` 只返回名称和描述，Profile 保存也接受任意工具名。 | 前端隐藏无法阻止伪造绑定，用户看不到风险、权限、生命周期和运行时停用原因。 | 工具选择功能早于 Tool Registry 治理和统一 Capability 合同。 | 新增 owner 目录服务，按租户、sub Agent 所有权和持久化 RBAC 权限过滤；设置保存再次校验稳定身份与可用性，目录保持只读且不持有执行器。 | 静态权限矩阵覆盖平台 Tool、default/租户 Skill、本人/他人 sub Agent、管理员和无权限主体；执行入口仍只有原 Tool Pipeline。 | 旧 `/available-tools` 为兼容保留，前端将在 4.3 切换到统一目录。 |
| P025 | Stage 4 | RESOLVED | 个人 Agent 设置只展示扁平 Tool 名称，无法说明 Skill、sub Agent、风险、审批和停用状态。 | 使用者无法判断 Harness 的能力边界，也可能把停用能力当作保存失败或运行故障。 | 前端消费的是旧名称/描述接口，没有统一 Descriptor。 | 新增紧凑 Capability Picker，按 Tool、Skill、Sub Agent 分组；风险和可用性同时使用图标、文字与颜色表达，未绑定的不可用 Tool 禁选，已绑定的停用 Tool 仍可移除。 | `npm run build` 通过；Playwright 脱敏目录在 1280x720 与 390x844 均无横向溢出或遮挡，移动弹窗标题栏经截图复查，控制台无错误。 | 第一版只允许直接编辑现有 Profile 的 Tool allowlist；Skill 和 sub Agent 展示运行时目录状态，不冒充已完成通用编排画布。 |
| P026 | Stage 4 | RESOLVED | 旧面试材料分散讲 RAG、ReAct 和工具名词，没有可重复准备、故障注入和证据检查步骤。 | 演示容易退化成口头讲架构，或把代码可调用误说成真实质量提升。 | 项目文档按模块组织，没有围绕同一个 Run 设计验收故事。 | 新增三条 Harness 演示路径，分别覆盖知识证据、审批恢复、预算终止；每条都列出准备、请求、预期证据、故障注入与不能宣称的边界。 | 接口、页面、环境变量、Tool 名和终止原因均与当前源码逐项扫描对齐。 | 三条路径仍需重启当前后端并使用真实账号/模型执行，最终运行结果不得由文档替代。 |
| P027 | Stage 4 | RESOLVED | 能力目录请求失败时，前端把失败响应降级为空目录，而 Profile 的历史空 Tool 列表语义是启用全部可用工具。 | 用户可能只修改名称或 Prompt 并保存，却在看不到能力边界时提交空列表，造成与界面认知不一致的工具授权。 | 加载逻辑没有区分“成功返回零能力”和“能力目录不可达”。 | 单独记录能力目录就绪状态；目录失败时阻止打开和保存 Agent 配置，提示刷新后重试，后端绑定校验继续作为最终边界。 | 前端生产构建与源码分支检查确认失败状态不能进入保存请求。 | 运行中的旧后端必须重启后才提供新目录接口，否则配置入口会按设计保持禁用。 |
| P028 | Stage 5 | RESOLVED | 统一 Capability 规格要求 Profile 按稳定身份绑定能力，但当前持久化和 Runtime 只有 Tool 名称列表与互斥单 Skill 字段，子 Agent 仅展示不可选择。 | 页面看起来像可组合 Harness，实际无法让一个 Agent 使用绑定 Skill 或委派子 Agent，且个人 `ORCHESTRATED` 模式直接报错。 | Stage 4 只完成 Descriptor、目录和展示，没有继续追踪到 Profile 数据合同与执行器。 | 新增显式统一绑定字段、运行时能力快照、受治理 Skill 适配器与有深度/环路保护的子 Agent 委派，并补齐四种模式语义。 | Stage 5 静态矩阵覆盖 NULL/[]、保存准入、稳定 ID Skill、三层防环委派、根 Run 治理、四模式同步/流式路径；前端混合绑定和零能力 fixture 均无损保存，生产构建通过。 | 真实父子模型调用、共享预算、取消和审批恢复仍需在重启后的后端中提供运行证据。 |
| P029 | Stage 5 | RESOLVED | 旧客户端不认识 `capabilityBindings`，更新新版 Profile 时可能把已绑定的 Skill 和子 Agent 静默清空。 | 用户只修改名称或旧 Tool 列表也会破坏组合配置，重新运行时能力边界发生非预期变化。 | 新旧请求共享同一个全量更新接口，但旧请求只能表达 `tools` 和互斥的单 `skillId`。 | 显式统一列表作为新合同权威值；旧请求更新已迁移 Profile 时只替换它实际提交的旧字段，并保留其余稳定身份；legacy NULL Profile 不强制迁移。 | 静态分支检查覆盖新建/更新、NULL/[]、省略 tools、替换 tools 和单 Skill 合并；`git diff --check` 作为格式门禁。 | 旧客户端无法展示组合能力，最终编辑体验仍由任务 5.6 切换到统一字段。 |
| P030 | Stage 5 | RESOLVED | 能力目录排除直接绑定自身，但不能发现 A 绑定 B、B 再绑定 A 的间接循环。 | 配置可以保存成功，运行时才不断委派并消耗模型、工具和 Token 预算。 | 目录是扁平可见性投影，不包含 Agent 引用图校验。 | 保存显式绑定时基于当前租户 Profile 构建可达引用图，用活动路径检测自引用和间接循环；损坏的可达子 Agent 配置按失败关闭处理。 | 静态 DFS 分支检查覆盖直接自引用、A-B-A、下游既有环、无关重复子图和 legacy 子 Agent。 | 并发更新两个 Profile 仍可能产生检查后写入竞态，运行时必须继续执行深度和活动路径双重保护。 |
| P031 | Stage 5 | RESOLVED | 保存期循环校验的初版通过 Capability Registry 暴露 JPA Profile 实体。 | 只读扩展目录会反向承担持久化图查询职责，模糊 Registry 与准入服务边界。 | 为复用 Registry 已持有的 Repository，临时增加了实体快照方法。 | 删除 Registry 实体出口，由绑定准入服务直接依赖 `AgentProfileRepository` 读取租户引用图；Registry 继续只输出 Descriptor。 | 依赖方向与任务文件清单复查通过，Registry 不再公开 `AgentProfile` 集合。 | AgentCapabilityService 同时承担目录过滤和保存准入，规模继续增长时可按查询/命令拆分。 |
| P032 | Stage 5 | RESOLVED | 子 Agent Descriptor 的风险与权限仍根据旧 `tools` 字段推导，显式 `[]` 被误判为旧语义“全部工具”。 | 零工具子 Agent 会显示错误风险，并可能因某个未实际绑定的高权限 Tool 而对普通用户不可见。 | Stage 4 Registry 早于统一绑定合同，只能依据 legacy Tool 列表计算。 | Descriptor 对显式 Profile 从 `capabilityBindings` 精确提取 Tool；只有统一字段为 NULL 时才保留旧空列表=全部工具规则。 | 静态分支检查确认 explicit [] 得到零 Tool，explicit Tool 列表精确映射，legacy NULL 保持原行为。 | Skill 与下层子 Agent 的传递风险尚未聚合，当前 Descriptor 只声明直接 Tool 风险。 |
| P033 | Stage 5 | RESOLVED | 审批恢复 worker 从 Checkpoint 直接继续 ReAct，绕过建立能力作用域的 ConfigurableAgentExecutor。 | Agent 在等待期间被撤销的 Tool 绑定仍可能作为模型规格暴露，Skill/子 Agent 适配器也无法恢复到正确执行层。 | Durable 恢复按历史 Tool 白名单和当前 RBAC 重授权，但当时系统还没有每层统一 Capability Scope。 | 按 Checkpoint `executorId` 定位原 Profile，在恢复 RunScope 内重建当前能力快照，收窄待审批调用和后续 ReAct 规格；无法定位、已停用或已撤权时失败关闭。 | 静态调用链确认 Profile 恢复依次取 Checkpoint allowlist、当前绑定快照和当前 RBAC 交集；空交集用精确过滤产生零规格，不调用旧空列表=全部工具重载。 | 非 Profile specialist 的旧 Checkpoint 继续使用原治理白名单，不冒充具备 Profile 组合能力。 |
| P034 | Stage 5 | RESOLVED | `SkillManager.processWithSkillByName` 找不到请求 Skill 时会执行默认 Skill，且稳定 ID 与显示名称共用别名 Map。 | 模型请求一个不存在或错误的 ID 时可能执行另一个 Skill，违反 Profile 精确 allowlist，也让审计记录与实际行为不一致。 | 旧 Skill 路由面向自然语言和兼容名称设计，允许默认兜底；组合 Agent 需要机器身份的确定性语义。 | 分离稳定 ID 索引并增加无默认兜底的精确执行入口；Agent Tool 先检查当前能力作用域，再按稳定 ID 调用。 | 静态调用顺序为 `AgentCapabilityScope -> allowsSkill -> processWithSkillById -> findSkillById -> Skill`，未绑定 ID 在进入 SkillManager 前拒绝，不存在 ID 不执行 defaultSkill。 | 斜杠命令和旧独立 Skill 路由仍保留名称/默认兜底兼容语义。 |
| P035 | Stage 5 | RESOLVED | Skill adapter 接受仅包含 `skill:` 前缀的参数，规范化后会得到空稳定 ID。 | 错误被误报为“未绑定”，参数合同和权限拒绝语义混在一起。 | 初版只在移除类型前缀前校验输入非空，没有校验移除后的 source ID。 | 前缀规范化后再次校验稳定 ID，空 source ID 在读取能力快照和进入 SkillManager 前按参数错误拒绝。 | 静态分支检查覆盖 null、空白、`skill:`、带前缀有效 ID 和裸有效 ID；`git diff --check` 作为格式门禁。 | LangChain4j Schema 仍只声明字符串类型，服务端校验继续作为最终边界。 |
| P036 | Stage 5 | RESOLVED | 同一稳定 ID 的默认 Skill 被普通热更新入口替换后，`defaultSkill` 仍可能指向已从两个索引移除的旧对象。 | 旧命令/自然语言兼容路径可能继续执行过期 Prompt，而精确 ID 路径执行新版本。 | `upsertSkill` 同步更新列表与两个索引，但没有同步默认指针。 | 替换集合包含当前默认 Skill 时，将默认指针原子切换到新运行时对象。 | 静态注册分支检查覆盖普通 Skill、默认 Skill 首次注册、同 ID 热更新和注销；所有索引与默认指针在同一写锁内更新。 | 显示名称冲突仍按现有替换语义处理，数据库层应继续保证租户内命名约束。 |
| P037 | Stage 5 | RESOLVED | `listAvailableSkills/useSkill` 同时作为普通 Tool 出现在能力目录和无 Profile 的默认 ReAct 工具集中。 | 用户可以错误地手工绑定 Harness 管道组件；没有能力作用域的旧 ReAct 调用 adapter 时又必然被拒绝。 | Skill adapter 最初按普通 `@Tool` 注册，统一能力目录和默认工具列表无法区分业务 Tool 与 Harness 内部桥接工具。 | 将两项标记为内部 Capability adapter，从用户可绑定 Tool 目录和默认 ReAct 规格中排除；Configurable Agent 仅在有效 Skill 集非空时通过精确规格入口自动加入。 | 静态调用链确认默认规格排除 adapter、能力目录不发布 adapter、Chat 不构造工具规格、ReAct 使用快照生成精确 Tool+adapter 白名单。 | 历史 Profile 若曾手工保存 adapter Tool，运行解析会按能力不存在排除；迁移期可在前端 5.6 中允许用户清理旧绑定。 |
| P038 | Stage 5 | RESOLVED | Skill Descriptor 只检查数据库启用和运行时加载，没有检查执行所依赖的 adapter 是否被 Tool 运行策略关闭。 | 页面允许绑定一个运行时必然无法调用的 Skill，保存成功但模型执行失败。 | Capability Registry 与 Tool Registry 的启用及风险策略没有在 Skill 投影时建立依赖关系。 | Tool 与 Skill 可用性都投影 `enabled-tools` 和环境最高风险；Skill 还要求稳定 ID 精确加载，且 `listAvailableSkills/useSkill` 均可执行，否则返回稳定不可用原因。 | 静态分支检查覆盖 Skill 停用、未加载、adapter 关闭、风险超限和完全可用；Tool Descriptor 同步使用相同风险上限。 | 配置在进程生命周期内不可热更新，能力快照与实际 Tool Registry 使用同一启动期策略。 |
| P039 | Stage 5 | RESOLVED | 委派 adapter 若沿用普通 Tool 执行池，父委派等待子 Agent 时会占住 Tool worker。 | 三层委派中的孙 Agent 再调用普通 Tool 时可能因 worker 全被父级委派占用而队列饥饿，最终只表现为超时。 | Tool Pipeline 的有界池适合叶子工具，但同步递归 Agent 调用具有“占用 worker 再等待下层工具”的不同阻塞拓扑。 | 委派 adapter 使用独立有界 `agentDelegationExecutor`，普通 Tool 保留原池；两个池共享 TaskDecorator 和完整 Tool Pipeline，委派超时取 adapter 上限与根 Run 剩余时间的较小值。 | 静态调用图确认执行器只在 Tool 实现调度点分流，Admission、`beforeToolCall`、取消、脱敏、Journal 和事件仍为共同路径；拒绝异常分类为 `POLICY_DENIED`。 | 两个池的容量仍需按真实并发和模型耗时压测调优；队列饱和会按工具失败安全返回。 |
| P040 | Stage 5 | RESOLVED | 保存层已经允许 `orchestrated`，但运行路由把所有非 Chat 显式模式折叠成 ReAct，Configurable executor 又直接拒绝 Orchestrated。 | 页面即使保存成功也无法真正调用绑定子 Agent，同步与流式结果与配置不一致。 | 模式枚举和保存校验先扩展，统一路由与 Profile executor 尚未完成对应执行分支。 | 显式模式统一由枚举解析；Auto 先分类，复杂任务再依据 Orchestrated 能力快照中是否有有效子 Agent 决定 ReAct/Orchestrated；Configurable 同步与流式均用相同精确 adapter 规则。 | 静态模式矩阵覆盖 Auto 简单/复杂、有效/无效子 Agent和三种显式模式；同步与流式只在 Chat 是否构造工具规格上分支。 | AUTO 分类仍是确定性启发式，真实路由质量需要 Stage 5 演示样本评估。 |
| P041 | Stage 5 | RESOLVED | 已绑定能力如果后来从当前用户目录中彻底消失，设置页只遍历目录项，无法展示该历史绑定。 | 用户既不能识别也不能移除失效身份，后续保存会携带未知绑定并被服务端拒绝，形成无法自助修复的配置。 | Picker 把能力目录当成唯一渲染来源，没有把 Profile 的权威绑定集合与目录做差集。 | 在 Picker 中为目录缺失但仍被选中的稳定身份生成只读失效占位项；占位项只允许取消选择，取消后不可重新添加。 | 浏览器 fixture 将 `tool:retiredTool` 回显为已选不可用项，移除后保存 payload 不再携带该身份；生产构建通过。 | 无权查看的能力与已经删除的能力统一显示安全摘要，不向前端补充其历史敏感元数据。 |
| P042 | Stage 5 | RESOLVED | 管理端详情把空 `tools` 一律显示成“全部（默认）”，没有区分 legacy `capabilityBindings=NULL` 与显式 `capabilityBindings=[]`。 | 零能力 Agent 会被误读为拥有全部工具，管理界面与 Runtime 权威语义相反。 | 详情页仍按旧 Tool 字段的空列表兼容规则渲染，未读取 `explicitCapabilityBindings`。 | 管理详情优先展示统一能力绑定；显式空数组标记为“未绑定能力”，只有 legacy Profile 的空 Tool 列表继续显示“全部工具（兼容模式）”。 | 管理详情静态分支和前端生产构建通过；个人设置 fixture 证明显式 `capabilityBindings=[]` 与 `tools=[]` 无损提交并回显为三类 0。 | 管理端本阶段只补准确可见性，不建设第二套组合编辑器。 |
| P043 | Stage 5 | RESOLVED | Agent 设置使用通用 `Field` 包裹 Capability Picker，外层与每个 checkbox 行都渲染为 `<label>`。 | 嵌套 label 是无效交互结构，点击分组空白或子项时可能关联错误控件，键盘和辅助技术语义也不稳定。 | 通用单输入字段组件被复用于一组复合控件，没有区分单控件 label 与 fieldset/group。 | 模式和能力目录改用独立表单分组容器，内部控件各自保留明确的可访问名称和 `role`。 | 浏览器 DOM 检查 `label label=0`；四模式暴露 radiogroup/radio，能力集合使用具名 group，每个 checkbox 有动作型名称。 | 其他页面如果也把复合控件放进 `Field`，需要在对应页面独立审计。 |
| P044 | Stage 5 | RESOLVED | 390px 视口下页面根宽度为 422px，虽然设置弹窗本身未溢出，遮罩后的应用壳仍产生横向滚动。 | 移动端关闭弹窗后页面可左右漂移，且不能以局部截图正常证明响应式验收。 | 单列响应式 Grid 使用 `1fr`，横向导航的 min-content 宽度把根轨道撑大；主区和侧栏没有在断点处显式允许收缩。 | 单列轨道改为 `minmax(0, 1fr)`，并将侧栏、主区限制为视口内宽度，导航继续只在自身容器滚动。 | 390x844 实测 documentWidth=390、modal client/scrollWidth=374/374；1280x720 实测 documentWidth=1280、modal client/scrollWidth=920/920，截图无遮挡。 | 其他后台密集表格仍可能在其自身容器内滚动，但不应撑大页面根宽。 |
| P045 | Stage 5 | RESOLVED | 原 5.7 文件清单没有包含 `sdd-assumptions.md`，但 A009/A010 仍将已经实现的统一绑定和根 Run 治理标为待实现。 | 即使产品文档更新，验收状态仍会自相矛盾，无法证明 Stage 5 的假设是否成立。 | 5.7 规划时只枚举了对外文档和阶段状态，没有把活跃假设日志纳入完成审计。 | 将 5.7 拆为产品文档同步与静态验收状态两项，每项不超过 5 个文件；验收项同时更新假设、报告和 SDD 状态。 | A009/A010、SC-9 至 SC-12、问题日志、任务计数和阶段状态已经同步；前端生产构建、`git diff --check`、过时状态扫描与 OpenSpec strict validation 均通过。 | A010 明确保持“静态确认、运行未验证”，真实嵌套 Run 不由文档一致性代替。 |
| P046 | Runtime acceptance | RESOLVED | `localhost:8080` 健康，但实时 v1 OpenAPI 没有新 `/api/v1/my/capabilities` 和 `/api/v1/agent-evals/**`；旧 Swagger 标签页还缓存过已删除的管理员申请接口。 | 当前实例不能用于证明统一能力绑定、四模式、Agent Eval 和子 Agent 委派；成功请求也只代表旧后端。 | Java 应用进程启动于 11:03:58，而 Capability/Eval Controller 源码分别在 13:21/12:47 才写入；对应 class 不存在于 `target/classes`，前端静态产物更新也不会热替换 Java 类。 | 使用 JDK 17 从当前工作区重新启动后端，再以新 OpenAPI 中存在 `/api/v1/my/capabilities` 和 `/api/v1/agent-evals/**`、不存在 `/api/v1/admin-role-requests` 作为版本基线。 | 用户在 11:29 重新构建后，Capability、Agent Eval 和 SkillManager class 已同时生成；新启动已进入修改后的 Skill 注册路径，旧 8080 进程也已退出。 | 新进程随后暴露 P048 并启动失败；OpenAPI 基线仍需在 P048 修复后重新确认。 |
| P047 | Stage 5 documentation | RESOLVED | `CLAUDE.md` 仍把普通用户工作区列为“我的 Agent、知识库、记忆、文件”四个入口，实际 `userNavGroups` 已经只有“我的 Agent、知识、上下文”三个入口。 | 后续开发者与面试演示会按错误的信息架构理解当前产品，SC-1 的三入口证据也与项目总说明冲突。 | Stage 1 收敛前端导航时没有同步项目级开发说明，Stage 5 的文档扫描范围又未包含 `CLAUDE.md`。 | 将项目说明改为当前三个入口，并明确文件已经作为知识来源并入“知识”。 | `CLAUDE.md`、`userNavGroups`、Harness 文档与 SC-1 现在均为三个入口；术语扫描和 `git diff --check` 通过。 | 后续导航变更仍需同时更新项目说明、Harness 文档和验收报告。 |
| P048 | Runtime acceptance | RESOLVED | 当前源码首次启动加载数据库普通 Skill 时，`SkillManager.upsertSkill` 在默认 Skill 尚未注册的情况下执行 `replaced.contains(null)`，导致 `CommandLineRunner` 抛出 NPE 并终止应用。 | 新版本后端无法完成启动，Capability、Agent Eval 和组合 Agent 均无法进行真实验收。 | `replaced` 由 `Collectors.toUnmodifiableSet()` 生成，JDK 17 不允许该集合使用 `null` 执行 `contains`；新增默认 Skill 热更新逻辑没有覆盖初始化顺序中 `defaultSkill == null` 的分支。 | 在检查替换集合前显式判断 `defaultSkill != null`，保留已存在默认 Skill 被同 ID/同名替换时同步指针的行为。 | 用户使用当前源码重新启动后，`8080` 监听正常，`/actuator/health` 返回 `UP`；实时 v1 OpenAPI 已包含 `/api/v1/my/capabilities` 和 3 个 `/api/v1/agent-evals/**` 路径，并且不再包含 `/api/v1/admin-role-requests`。 | 未运行自动化测试或 Maven编译；运行证据确认启动缺陷已闭环，后续真实业务链路验收单独记录。 |
| P049 | Runtime acceptance | RESOLVED | 当前源码启动成功且两类账号均可登录，但普通用户和管理员请求 `/api/v1/my/capabilities` 都返回 HTTP 500；同一普通用户的 `/api/v1/my/agents` 正常返回。 | 个人 Agent 设置无法加载统一 Tool/Skill/sub-Agent 能力目录，Stage 5 的真实保存回显和后续组合 Run 无法验收。 | Tool Descriptor 将 LangChain4j 1.19 的 `JsonObjectSchema` 直接放入 HTTP `inputContract`；该 SDK 类型没有 Jackson Bean 属性，响应序列化因 `FAIL_ON_EMPTY_BEANS` 失败。 | 在 Capability Registry 的边界使用 LangChain4j Schema 转换器生成只含 JSON 原生 Map/List/标量的合同；保留全局 Jackson 严格校验，不用关闭 `FAIL_ON_EMPTY_BEANS` 掩盖问题。 | 完整异常精确指向 `inputContract["schema"]`；源码检查确认对外 Descriptor 不再持有 `JsonObjectSchema`，`git diff --check` 通过。 | 未按用户约束编译或运行自动化测试；需重启后确认两类角色均返回 200，并检查目录内容与设置页回显。 |
| P050 | Product UX | RESOLVED | 普通 Agent 设置默认展开全部执行模式、精确能力绑定以及 16 个 Tool 的风险、可用性、稳定 ID 和版本信息。 | 用户首先面对 Harness 控制面复杂度，个人 Agent 的核心配置不清晰，弹窗过高且难以扫描。 | 基础身份配置与专家级运行治理共用一个始终展开的表单，没有按用户任务分层。 | 使用人类可读的能力包承载常用选择，将执行模式和精确 Tool/Skill/子 Agent 绑定收进折叠的高级设置；详细目录增加搜索、类型筛选、已选优先和按项展开。 | `npm run build` 通过；真实登录页面验证默认只展示四个能力包，高级设置可展开、搜索和查看技术信息；391x844 实测 document/modal scrollWidth 为 391/375，`label label=0`，控制台无错误。 | 能力包通过当前稳定 Tool 名称归类；新增平台 Tool 需明确加入能力包，否则仍可在高级设置中配置。 |
| P051 | Runtime planning | STATICALLY_RESOLVED | 普通请求的模式、RAG、记忆和工具暴露由多个执行组件分别决定，`你能干什么` 也可能先检索知识并把完整 Tool Schema 发送给模型。 | 简单请求产生不必要的延迟和 Token 成本，Trace 也无法解释本次为什么使用这些资源。 | 系统已有 Agent Run 和 Capability 治理，但缺少模型调用前唯一、不可变、可留证的请求计划。 | 新增规则优先的 `RequestExecutionPlan`，统一决定模式和最小资源；能力介绍由 Harness 本地返回，候选工具再与 Profile、RBAC、运行策略和审批治理求交集，低置信度显式回退到本 Agent 已授权能力。 | 静态调用链确认同步/SSE 共用计划，Chat 不构造工具规格，RAG/记忆按计划跳过，Skill 与委派 adapter 优先保留；`git diff --check` 通过。 | 路由规则、Skill 名称/描述匹配和 5 个候选上限仍需用真实请求集评测；当前不宣称已降低线上平均 Token 或提升任务成功率。 |
| P052 | Runtime acceptance | STATICALLY_RESOLVED | 新请求计划接入后，Spring 启动时形成 `AgentToolRegistry -> AgentTools -> AgentDelegationToolService -> PersonalAgentExecutionModeResolver -> PersonalAgentRequestPlanner -> AgentCapabilityService -> AgentCapabilityRegistry -> AgentToolRegistry` 循环依赖。 | 应用在数据库、Embedding 和 Milvus 初始化成功后仍无法完成 ApplicationContext 创建，所有 Agent 接口不可用。 | 委派 Tool 在注册阶段通过构造器提前获取执行模式解析器，而该解析器的规划链最终依赖尚在创建中的 Tool Registry；实际只有执行 `delegateToAgent` 时才需要解析器。 | `AgentDelegationToolService` 改为注入 `ObjectProvider<PersonalAgentExecutionModeResolver>`，仅在真正委派子 Agent 时获取解析器，移除启动期依赖闭环。 | 静态依赖图确认 Registry 创建链不再提前实例化 Resolver；`git diff --check` 与 OpenSpec strict validation 通过。 | 按用户约束未编译、未运行自动化测试、未代启后端；需要 IDEA 重新构建并启动，确认真实 ApplicationContext 可以完成初始化。 |
