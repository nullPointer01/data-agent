# Agent Harness 当前进度与验收交接

> 更新时间：2026-09-10  
> 用途：作为新会话的唯一恢复入口，先验收再修复，不再重新设计产品方向。

## 一、已冻结的产品定位

Data Agent 是一个用于 Agent 开发岗求职展示的 **Java Agent Harness 参考应用**：

> 用户向自己的个人 Agent 提出问题或任务；Agent 自动判断是否使用模型、个人知识、工具或专家 Agent；结果带可核对的引用和运行证据；高风险动作先暂停并等待审批；管理员在 Control Plane 治理模型、能力、权限和运行质量。

它不是单一的企业问答 Agent，也不是把所有技术概念都暴露给普通用户的管理后台。普通使用面保持简洁，Harness 的工程证据收进管理控制面。

## 二、当前产品结构

### 普通用户

- **我的 Agent**：正式对话入口，配置名称、模型、System Prompt、能力包和已接入专家。
- **专家助手**：创建非默认 Agent Profile，作为受控子 Agent 供个人 Agent 委派。
- **知识**：录入文本或上传知识文档，进入个人 RAG 索引。
- **上下文**：管理会话与长期记忆。

个人 Agent 只暴露四类业务能力包：知识与记忆、文件理解、数据分析、专业技能。页面固定保存 `executionMode=auto`，普通用户不选 Chat/ReAct/Orchestrated、Token 预算和 Tool Schema。

### 管理员 Control Plane

- 运行与质量：运行追踪、动作审批、质量观测、Agent 评测、资源统计。
- 能力配置：模型目录、技能目录、知识与 RAG、数据连接。
- 平台治理：用户、用户 Agent、角色权限、审计。

Harness 管理的是“一次 Agent Run 能如何执行”；管理员管理的是“平台允许哪些用户和能力存在”，两者不是同一概念。

聊天回答不再提供点赞/点踩。对应按钮、前端请求、管理页面、后端接口和质量评分依赖均已移除；质量分只根据执行成功率、耗时和回退率计算。旧库如需物理删除历史反馈表，可手动执行 `sql/drop-agent-feedback.sql`。

所有实际数据表格默认按 10 条分页，可切换为 20 或 50 条；专家助手卡片列表使用相同分页交互，数据不足一页时不展示分页控件。会话列表不分页，默认展示最近 5 条，可展开全部并收起。首页固定数量摘要、质量建议、风险项和能力选择器不属于全量数据列表，不做分页。

“用户 Agent”是治理清单，不是第二套用户配置入口。管理员可按归属用户和角色查看全部 Agent、查看详情并跳转到该用户的运行追踪；不可新增、编辑或删除用户 Agent，只可启用或停用专家助手。每个用户的默认主 Agent 继续展示，但不能停用或删除；用户在“我的 Agent”修改配置后，管理员刷新清单即可看到同一份 Profile 的最新状态。

## 三、代码已实现的五个目标

| 目标 | 已实现内容 | 运行时验收状态 |
|---|---|---|
| 最小必要调用 | `PersonalAgentRequestPlanner` 对“你能做什么”走本地回答，关闭模型、RAG 和 Tool | 已通过 |
| 知识证据 | Milvus 向量召回 + Elasticsearch BM25 + RRF + Reranker + Citation | 已通过 |
| 工具与专家委派 | Capability 白名单、`delegateToAgent`、父子 Agent 共享根 Run 预算与 Trace | 已连续通过 3 次 |
| 高风险审批 | 暂停、加密 Checkpoint、审批、恢复时重授权、沙箱幂等 | 完整链路已真实通过 1 次；申请人身份和用户端终态反馈已修复，待再连续验证 2 次 |
| 可观测运行 | Trace 持久化、请求计划、预算、候选工具、工具治理和终态证据 | 知识黄金 Run 已通过，工具与审批证据待后续验收 |

当前判断：**工程实现约 80%，可用于面试展示的运行证据约 40%**。后续不再加新概念，优先把五条链路跑通。

当前验收进度：第 1、2、3、4 条已通过；第 5 条高风险动作审批与恢复已真实通过 1 次，下一步按同一用例再连续验证 2 次。

### 上下文语义记忆闭环

记忆中心此前只有摘要，`PREFERENCE / ENTITY / CONCLUSION` 长期为空，并且旧画像从摘要正文跑正则，曾把 `super` 错误识别为“称呼=你的、角色=你的”。2026-09-10 已完成根因修复：

- 会话持久化成功后仍同步保存短期摘要；显式“记住”同步写语义记忆，普通对话异步调用 JSON 模型提取高置信度候选。
- 语义候选必须通过类型、稳定键、用户原文证据、置信度和敏感信息校验；同一属性按稳定键覆盖，不重复新增。
- 结构化画像只由长期 `PREFERENCE / ENTITY` 的合法键完整重建，不再读取摘要；无有效投影证据时删除旧画像。
- 记忆中心分类卡片已展示来源、置信度和更新时间，并支持当前用户修正或删除自己的长期语义记忆。
- 已执行 `sql/upgrade-semantic-memory.sql`，并精确删除 `super` 的 1 条错误派生画像；原始摘要未删除。

静态门禁已通过：JDK 17 干净编译、11 个语义记忆单测、前端生产构建和 `git diff --check`。独立 `18080` 实例已完成桌面端和 `390x844` 移动端验证：来源、置信度、更新时间、编辑/删除、画像联动和空状态均正常；临时 QA 记忆及派生画像已清理。后端源码仍需重启 IntelliJ 当前 8080 JVM 后再做真实对话验收。固定用例：

```text
请记住：以后回答保持简洁，优先使用表格
```

通过条件：生成或覆盖两条 `PREFERENCE`，结构化画像显示“简洁直接 / 表格优先”；再输入相反偏好时覆盖原记录而非新增。详细链路见 `docs/核心逻辑详解/用户语义记忆.md`。

## 四、固定验收顺序

### 1. 本地能力介绍

在“我的 Agent”新对话输入：

```text
你能做什么
```

然后进入 `Control Plane -> 运行追踪`，打开最新 Trace。通过条件：

- 模式 `CHAT`，意图 `CAPABILITY_INTRODUCTION`，规则 `capability-introduction`。
- 模型、知识库、记忆和历史都关闭，候选工具为空。
- `modelCalls=0`、`toolCalls=0`、`tokens=0`、终态 `COMPLETED`。

### 2. 个人知识召回与引用

在“知识 -> 添加文本”录入：

```text
名称：星河账户风控规则
描述：用于验证个人知识库召回
内容：星河系统账户风控规则：手机验证码在30分钟内连续输错2次只记录风险日志，不会限制登录。连续输错第5次时，账户会被限制登录15分钟。人工解除限制时需要提供解除码 YM-2609。
```

先用“知识 -> 检索测试”确认索引存在，再用 `Control Plane -> 知识与 RAG -> 检索测试` 检查 Milvus 向量命中和 Elasticsearch 全文命中。

在“我的 Agent”输入：

```text
手机验证码输错两次会触发限制吗？如果不会，第几次触发，限制多久？
```

通过条件：答案为“两次不限制、第五次触发、限制 15 分钟”，页面显示“参考了你的知识库”，引用来源是“星河账户风控规则”。Trace 应显示 `PERSONAL_KNOWLEDGE + CHAT + ragRequired=true`。

### 3. 同一 Run 的 Harness 证据

直接使用第 2 条产生的 Trace，不需要再发问题。通过条件：

- 有请求意图、模式、置信度、命中规则和资源开关。
- 有模型调用数、工具调用数、Token、耗时和终止原因。
- `runId` 与 `traceId` 使用同一标识。
- 终态为 `COMPLETED`，工具调用为 0，模型调用和 Token 大于 0。

### 4. 专家 Agent 委派

在“专家助手”创建：

```text
名称：酒店经营分析专家
简介：负责酒店经营指标查询和经营诊断
角色与要求：你是酒店经营分析专家。必须先获取经营数据，再计算指标变化，最后给出有数据依据的建议。
专长：数据分析
```

然后到“我的 Agent -> 设置 -> 专家助手”启用它，专家卡片应显示“已接入”。输入：

```text
请把任务委派给酒店经营分析专家：先查询杭州上周酒店入住率，然后计算与前期的变化，最后总结经营建议。
```

通过条件：模式 `ORCHESTRATED`，候选工具包含 `delegateToAgent`，工具治理证据中真实出现 `delegateToAgent`，子 Agent 可继续调用 `queryHotelOccupancy`，父子执行共享根 Run。

### 5. 高风险动作审批与恢复

确认应用使用默认 `local` Profile 后重启即可，不需要在 IDEA Run Configuration 维护环境变量。使用两个不同账号：普通用户发起，管理员审批。发起用户的个人 Agent 需开启“专业技能”。输入：

```text
把演示酒店 HZ-001 的豪华大床房在 2026-09-15 的价格改成699元。
```

通过条件：

- 审批前 Run 为 `WAITING_APPROVAL`，页面提示需要人工审批。
- 审批前 `hotel_rate_sandbox` 没有对应记录。
- 另一个管理员在 `Control Plane -> 动作审批` 批准，发起人不能审自己的动作。
- 等待最多约 15 秒后，原 Run 恢复为 `COMPLETED`。
- 沙箱表只有 1 条对应动作，`after_price=699.00`，重放不重复写入。

## 五、执行纪律

1. 严格按 1 -> 2 -> 3 -> 4 -> 5 验收，不跳步。
2. 首次只跑 1 次；失败则只修当前链路，不增加新概念、不重构无关代码。
3. 修复后同一链路连续通过 3 次，再进入下一条。
4. 所有面试表述只基于实际跑出的页面、SSE、Trace 和数据库证据。
5. 失败时记录：验收编号、输入、用户页面结果、Trace 的“请求计划 / Run 预算与终态 / 工具治理”、后端最后一段完整异常。
6. 定位根因后的修复必须保持职责清晰和口径一致；禁止硬编码验收样例、简单调低阈值、复制同类逻辑或用兼容分支掩盖根因。

## 六、已知状态与注意事项

- 用户已确认最近一次后端可以启动；不代表五条业务链路已验收。
- Elasticsearch 和 Milvus 最近一次 Docker 状态正常；新会话仍应以 RAG 健康页和真实检索为准。
- 最新前端已重新构建到 Spring Boot 静态资源；后端源码变更仍需重启当前 IDEA 进程后生效。
- `local` 和 `prod` Profile 已显式开启 durable、sandbox tool 和 `updateHotelPrice`，两者均使用固定 YAML 配置。
- 当前进程启动时间早于上述 local 配置变更；开始第 5 条前需要重启应用使其生效。
- 第 5 条首次运行曾失败：规划器已得到 `ACTION + updateHotelPrice`，但模型只回复“请确认”且未产生 Function Calling，因而没有审批单。请求规划器 v2 修复后，真实请求已成功进入 `WAITING_APPROVAL`、由管理员批准并恢复为 `COMPLETED`，沙箱执行状态为 `SUCCEEDED`。审批接口和页面现已展示申请人/审批人的用户名与昵称；聊天页会在 `WAITING_APPROVAL/RESUMING` 期间自动刷新，并按等待、恢复、成功或失败显示最终状态。
- 当前工作树有大量未提交变更，不得回滚、清理或覆盖与当前验收无关的文件。
- 用户偏好：不使用 OpenSpec；未明确要求时不跑测试和编译。

## 七、新会话的第一个动作

新会话先读本文档，不做代码修改。让用户执行第 1 条验收，并提供：

1. “你能做什么”的回答截图。
2. Trace 详情中“请求计划”截图。
3. Trace 详情中“Run 预算与终态”截图。

根据证据判定通过或只修复该链路的阻断问题。

## 更新记录

- 2026-09-10：完成语义记忆页面运行时验收：移动端无横向溢出，弹窗在视口内完整展示；将“表格优先”修正为“列表优先”后画像实时重建，删除两条 QA 记忆后偏好与画像均归空。数据库确认 QA 记忆、`super` 语义记忆和派生画像均为 0，已索引的 QA 向量通过正常删除链路移除。
- 2026-09-10：完成用户语义记忆闭环：显式/隐式提取、稳定键覆盖、证据与敏感信息校验、画像纯投影、用户纠错；数据库已升级并清除 `super` 的错误派生画像，静态门禁通过，待重启后端做真实对话验收。
- 2026-09-10：完整移除聊天点赞/点踩及 Agent 反馈管理链路，质量评分改为纯运行指标；为数据表格和专家列表增加分页且不足一页时隐藏，会话列表改为加载更多/收起；修复 Token 柱状图坐标裁切、技能消耗单色大圆和质量趋势日期重叠。`mvn clean test` 通过，独立 8081 实例确认旧反馈接口返回 404，桌面及 390px 分页布局已验证。
- 2026-09-10：第 5 条真实审批与恢复完整通过 1 次：申请人 `testuser2`、审批人 `super`、Run 终态 `COMPLETED`、审批执行状态 `SUCCEEDED`，原会话消息已更新为最终结果。随后修复聊天页不轮询审批 Run、一直停留在“等待动作审批”的问题，并收紧“执行动作”统计口径；管理员申请人展示和用户端成功状态已通过浏览器验收，按验收纪律还需用同一用例再连续验证 2 次。
- 2026-09-10：第 5 条审批前半段真实验收通过，Run 成功进入 `WAITING_APPROVAL` 且管理员可以看到审批单；随后修复审批页只展示用户 UUID 的问题，服务端批量解析同租户用户名/昵称，前端主显示可读身份并保留 UUID 审计信息。代码静态检查完成，待重启/前端构建后验证展示，再继续批准恢复。
- 2026-09-10：修复高风险动作只在对话中请求确认、未创建审批单的问题；请求规划器 v2 为筛到唯一候选工具的精确 `ACTION` 固化首轮 `ToolChoice.REQUIRED`，后续工具观察轮和审批恢复轮保持 `AUTO`，同时收紧审批工具提示合同。代码静态检查完成，真实审批与恢复仍待重启后验收。
- 2026-09-09：环境开关最初按 Profile 收口；随后按当前部署方式调整为 local/prod 均默认开启审批沙箱，并移除 dev Profile。
- 2026-09-09：配置职责进一步收口；只保留 local/prod 两个 Profile，`application.yml`、`application-local.yml` 与 `application-prod.yml` 全部使用显式字面值，prod 当前与 local 保持一致。
- 2026-09-09：修复个人专家目录权限边界；默认主 Agent 不再注册为子 Agent，管理员个人工作区不再跨用户查看或绑定专家。第 4 条专家委派已连续通过 3 次，下一步验收第 5 条高风险审批。
- 2026-09-09：第 2、3 条验收通过；记录根因修复的代码质量纪律，并修正 RAG 即时相关性评估口径。
- 2026-09-09：将 Control Plane 的 Agent 管理收口为“用户 Agent”治理清单，补充管理员操作边界，并记录第 1、2 条验收进度。
- 2026-09-08：冻结产品定位和五条验收链路，建立新会话交接基线。
